[package]
name = "replay-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "bar-replay"
path = "src/main.rs"

[dependencies]
replay-core = { path = "../core" }
replay-data = { path = "../data" }
chrono = { workspace = true }
chrono-tz = { workspace = true }
